// Scaffolding only — future home for the achievement/badge system from the
// original roadmap notes (personal bests, skill-tied badges keyed to true-odds
// calls rather than volume, percentile framing over raw rank). Populating
// this array is the only change needed once they exist; UnlocksScreen
// already renders whatever's here.
export const BADGES = [];
